(ns xero-syncer.constants.topics)

(def sync-local-item "sync.local.item")
(def sync-remote-item "sync.remote.item")

(def sync-local-company "sync.local.company")
(def sync-remote-company "sync.remote.company")

(def sync-local-sales-order "sync.local.sales-order")
(def sync-remote-sales-order "sync.remote.sales-order")